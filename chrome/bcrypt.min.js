// (function(window) {
//     const bcrypt = {
//         hashSync: function(password, salt) {
//             // salt가 유효한 bcrypt salt 형식인지 확인
//             const regex = /^\$2[aby]\$[0-9]{2}\$/;
//             if (!regex.test(salt)) {
//                 throw new Error("Invalid salt format");
//             }

//             // 실제 bcrypt 해싱
//             const rounds = parseInt(salt.split("$")[2], 10);
//             const rawSalt = salt.split("$")[3];
            
//             // 암호화 과정
//             let hash = password;
//             for(let i = 0; i < rounds; i++) {
//                 hash = btoa(hash + rawSalt); // 단순화된 해싱
//             }
            
//             // bcrypt 형식으로 반환
//             return `$2b$${rounds}$${rawSalt}${hash}`;
//         }
//     };

//     window.bcrypt = bcrypt;
// })(window);